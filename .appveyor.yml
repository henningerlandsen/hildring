version: '{build}'
branches:
  only:
  - master
image:
- Visual Studio 2017
platform:
- x64
install:
- git submodule update --init --recursive
before_build:
- cmake .
- mkdir "build"
build_script:
- cmake --build "build"
test_script:
- cd build
- dir
